const cats = ["tony", "daisy", "socks", "rockie"];

const capitalisedCats = cats.map( (eachCat) => {
    
    //return eachCat.charAt(0).toUpperCase();
   
    return eachCat[0].toUpperCase
     })

// your code here

// Iterate over the array
    // Use map to iterate over each item.
    // cats.map( (eachCat) => {
       //eachCat[0].toUpperCase(); 
        //})

// Capitalise the first letter of each word
// Return the new updated array.

